本文件夹下的所有文件名都在源码的基础上添加后缀_rewrite

问题：
    1) 源码版本在拷贝时采用深拷贝方式，是否还具有快速性的优势？
    2) 在比较两个Json对象是否相等或不相等时，可以简单的比较指针是否相等或不相等吗？
    3) 已经定义了类型转换运算符，并且在定义时没有定义为const成员函数，还有必要再定义
        as_bool、as_int、as_double、as_string这四个成员函数吗？